# Simple News Scraper

A lightweight Python web scraper that extracts top headlines and exports them to a structured CSV file.

## Key Features
- Fetches real-time data from news portals.
- Parses HTML using BeautifulSoup.
- Automatically generates a timestamped CSV report.

## Requirements
Install dependencies using:
```bash
pip install -r requirements.txt
